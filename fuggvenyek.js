export function szamlalas(szam) {
  let valasz;
  // if (szam % 2 === 0) {
  //   valasz = true;
  // } else {
  //   valasz = false;
  // }
  szam % 2 === 0 ? (ps = true) : (ps = false);
  return valasz;
}
